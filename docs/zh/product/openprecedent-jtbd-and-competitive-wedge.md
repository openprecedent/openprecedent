# OpenPrecedent 待完成任务与竞争切入点

日期：2026-03-25
状态：草稿中的持续维护文档
英文对应稿：[docs/product/openprecedent-jtbd-and-competitive-wedge.md](/root/.config/superpowers/worktrees/openprecedent/codex-jtbd-product-wedge/docs/product/openprecedent-jtbd-and-competitive-wedge.md)

## 为什么写这份文档

这份文档用于固化一轮正在进行中的产品讨论：OpenPrecedent 到底应该替用户完成什么待完成任务（Jobs To Be Done，JTBD）。

这里真正要解决的问题，不是 Agent 能不能产生日志、追踪信息（tracing）或记忆检索结果，而是项目演进和 Agent 服务过程中，关键决策信息会持续流失。

这些缺失的信息可能出现在：

- 会议纪要里
- Slack 或其他即时通讯（IM）线程里
- 最终代码变更之外的仓库讨论里
- 客户特异性的例外要求里
- 只能靠专家回忆、却没有被清晰写下来的历史判断里

一旦这些决策信号消失，后续工作就容易滑向“看起来合理、其实错误”的通用解，重复已经否掉的方向，或者忽略仓库特异性和客户特异性约束。

因此，OpenPrecedent 应该围绕这个问题来评估，而不是围绕一个泛化的“Agent 可观测性”分类来评估。

## 问题界定

用户并不是为了拥有一个决策数据库才需要 OpenPrecedent。
用户真正需要的是：可靠地找回并复用过去工作背后的判断结构。

更具体地说，风险不只是 Agent 忘记事实。
更深的风险是 Agent 或后来的操作者忘记了：

- 当时为什么选这条路
- 当时有哪些关键约束
- 哪些方案被认真考虑过并最终否掉
- 哪些审批边界或权限边界曾经起作用
- 哪些客户或项目必须走非标准例外

如果这些信号缺失，后续迭代即使看起来专业，也可能在方向上持续偏离。

## 哪些决策最值得沉淀

最有价值的先例，通常不是那些已经被代码、文档或通用最佳实践充分表达出来的标准选择。
最有价值的先例，反而是那些偏离默认路径、后来又很难重新推回来的决策。

当前的工作假设是，应优先沉淀这几类决策：

- 脱离最初设计路线的调整
- 缺陷修复过程中的权衡与取舍
- 客户特殊要求带来的例外选择
- 某些版本因为时间压力而做出的裁剪决定
- 没有完整停留在文档和代码里的架构选型细节
- 缺陷修复、事故处理过程中采用的临时方案与过渡方案

这些决策特别值得沉淀，是因为它们往往同时具备几个特征：

- 重新发现它们的代价很高
- 它们在代码或最终文档中的表达非常弱
- 它们会深刻影响后续维护和演进
- 一旦人员流动或角色变化，就特别容易丢失

这套判断并不只适用于开发过程。
如果进一步泛化，类似逻辑也适用于：

- 改变路线或范围的重要产品规划决策
- 影响承诺边界的重要销售或客户交付决策

所以更一般化的一句话是：

OpenPrecedent 最应该优先沉淀的，是那些最难从最终产物里反推回来、却又最影响后续判断的决策。

## 候选待完成任务

### 候选主任务：新决策时的判断继承

当用户或 Agent 需要在真实项目里做出一个有分量的新决策时，他们需要一份简短但高密度的视图，告诉他们过去类似情境已经证明了哪些任务框定、约束条件、代价权衡、权限边界和成功标准是重要的。

用待完成任务句式表达就是：

当我需要在真实项目里做一个带有真实权衡的新决策，例如选方案、算成本、平衡速度、维护负担、审批代价或客户特异性例外时，我需要一份来自相似历史案例的判断结构简报，这样我就能用更少猜测、更少返工和更低风险推进当前工作，而不是重复走已经被否掉的路径。

规划、写入、恢复和审批边界这些时刻仍然是重要触发点，但它们不是全部。
更深一层的任务，是在真实项目权衡下支持“新的决策”本身，而不只是守护执行节点。

这是当前最强的产品切入点候选，因为它发生在用户最愿意改变行为、也最愿意在制造新的下游代价之前参考历史判断的时刻。

### 次任务：事后解释决策

当一次 Agent 执行结果看起来可疑时，用户需要快速还原某个决策为什么发生，以及判断到底错在什么地方。

这个任务很有价值，但单靠它不太适合作为最强切入点，因为许多可观测性工具已经部分覆盖了相邻的事后复盘需求。

### 长周期任务：持续沉淀可复用先例

随着时间推移，团队会希望把项目特异性和客户特异性的判断模式沉淀成可复用的先例，而不是散落在部落记忆里。
这里的关键不是一步到位替代专家判断，而是在真实工作过程中先辅助专业人员与 Agent 协作，再逐步把可复用的判断沉淀出来，降低对脆弱人工回忆的依赖。

这个任务同样重要，但它更像长期复利收益，而不是最窄、最容易打穿的首个产品切口。

## 用户真正“雇用”的是什么

OpenPrecedent 不应该把自己理解成单独售卖回放、存储或检索。

用户真正“雇用”它，是为了：

- 在下一步关键动作前降低决策风险
- 避免重复已经被否掉的推理
- 浮现仓库特异性和客户特异性的例外条件
- 缩短“我们以前做过类似事情”和“这次还应该继承哪部分判断”之间的距离

当产品表现得像一层判断继承基础设施，而不只是一个历史查看器时，它的价值会更强。

## 替代方案

用户今天已经有很多方式可以完成这项任务的一部分。
这些替代方案才是真正的竞争对手。

### 1. 不消费

用户什么都不额外做，直接让 Agent 继续往下走，依赖直觉、运气或事后人工复盘。

这往往是默认竞争对手，因为它的流程成本为零。

### 2. 人的回忆与专家咨询

用户直接去问维护者、产品负责人、客户侧操作者，或者那个“记得当时为什么这么决定”的专家。

这通常是最强替代方案，因为人比当前工具更擅长重建意图和例外。
它同时也是 OpenPrecedent 最需要长期替代掉的替代方案。

原因不是战术性的，而是结构性的：

- 关键决策信息往往只活在专家记忆里
- 获取专家回忆既慢又贵
- 人员流失和组织变更会让这套记忆基础越来越脆弱
- 项目越长期演进，越难持续维护这种“必须问对人”的依赖

OpenPrecedent 不应该假设自己能一步替代专家判断。
更合理的路径是：把专家与 Agent 的反复交互逐步沉淀成显式先例，让系统随着语料积累而降低对专家回忆的依赖。

### 3. 代码搜索与仓库历史

用户通过代码、拉取请求（PR）、议题（Issue）、提交（commit）、评论和文档去反推出以前发生过什么。

对于被清楚写下来的决策，这种方法还算有效；但对隐性的判断和对话外的上下文就比较弱。

### 4. 可观测性与追踪工具

这类工具擅长回答 Agent 做了什么。
但它们并不天然擅长回答：当前情境下，到底应该复用哪种判断结构。

### 5. 记忆、检索增强生成（RAG）与笔记检索系统

这类系统可以把相关事实、片段和文档找出来。
当缺失信号本来就存在于 Slack、会议笔记或客户资料中时，它们是很现实的替代方案。

它们的弱点在于，往往返回的是内容，而不是蒸馏过的决策谱系。

### 6. 流程模板、政策文档与检查清单

有些团队会把过往经验写成操作手册或评审清单。

它们能较好处理稳定、重复性的约束，但通常保不住“为什么某个例外在某个客户或某个仓库里是合理的”这种情境化细节。

## 用户为什么会选择 OpenPrecedent

只有当 OpenPrecedent 在下列几个方面比替代方案组合做得更好，用户才更可能真正“雇用”它：

- 比翻阅 PR、文档、聊天和笔记更快地返回相关判断
- 返回的是判断结构，而不是一堆相关文本
- 出现在规划、写入或失败恢复这些关键时刻，而不是只在事后出现
- 能保住仓库特异性和客户特异性例外，而不是把一切压平成通用最佳实践
- 能解释为什么某条先例相关，使结果具备可审计性和可信度

如果 OpenPrecedent 无法打赢“搜索 + 人工回忆”这个组合流程，它就很难变成稳定习惯。
更重要的长期门槛，是它能不能逐步吸收足够多可复用的专家判断，让组织越来越不依赖“记得去问谁”。

## 用户为什么可能不选择 OpenPrecedent

在下面这些情况下，用户很可能还是会选择替代方案：

- 任务太小、风险太低，不值得多走一步
- 几乎没有值得参考的历史
- 产品返回了太多原始历史，蒸馏过的指导太少
- 最关键的决策信息仍然在已捕获语料之外
- 用户更信任人类专家，而不是检索到的先例
- 产品看起来像另一个可观测性面板，而不是行动时辅助工具

这也意味着，“把回放做得更好”本身很可能不够。

## 当前产品与未来目标

## 当前已经验证的价值

当前 OpenPrecedent 的最强能力主要在于：

- 捕获本地案例历史
- 抽取决策记录
- 回放并解释一个案例
- 从已有历史中检索语义上相关的先例

这些能力非常重要，但更适合被理解为“证明这条产品路径可行”的基础设施底座。

## 未来目标价值

更强的未来产品目标应该更窄、更明确：

- 在下一次关键动作前浮现决策谱系
- 整合不止来自原始运行时转录的决策相关信号
- 把客户特异性和仓库特异性例外沉淀成一等先例
- 帮助 Agent 继承已经被证明有效的判断结构，而不是只检索邻近文本或操作相似性
- 把专业人员与 Agent 协作过程中形成的经验持续沉淀成可复用先例，让组织获得复利，而不是每次都退回部落记忆

未来产品不应该滑向通用图谱、通用记忆平台或通用追踪查看器。
它真正的独特价值，是在真实项目约束下完成决策继承。
它也不应该宣称自己能立刻替代专业判断。
更强的表述是：把可复用的专家决策逐步外化为可持续积累的先例层。

## 当前工作中的产品论点

OpenPrecedent 应该朝着这样一层能力演进：在做出下一次重要新决策之前，帮助 Agent 与操作者继承正确的历史判断，尤其是在最重要的上下文被切碎地分布在运行时历史、人类讨论和项目特异性例外中的情况下。

它的战略路径不是一步替代专家，而是在真实的专家-Agent 协作过程中不断捕获先例，让系统对可复用决策的沉淀形成复利，并逐步降低组织对脆弱人工回忆的依赖。

## 开放问题

- OpenPrecedent 未来应该直接吸收多少对话外上下文，又应该间接引用多少？
- 哪一个高频触发时刻最值得成为最小切入点：规划前、写入前、失败恢复后、审批边界处，还是更广义的方案权衡节点？
- 产品应如何区分稳定先例与一次性的本地例外？
- 在不滑向通用客户关系管理（CRM）或通用知识库的前提下，客户特异性例外应该如何建模？
- 什么样的证据才能证明“新决策时的决策谱系”真正改变了后续决策，而不只是让事后解释更顺？
- 哪些专家判断最有可能被优先外化成先例，哪些判断又会在更长时间内继续依赖专家本人？
